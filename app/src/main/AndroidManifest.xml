<manifest package="com.angeluz.freyja" xmlns:android="http://schemas.android.com/apk/res/android">
  <application
      android:label="La Voz de Freyja (Standalone)"
      android:allowBackup="true"
      android:supportsRtl="true"
      android:theme="@style/Theme.Freyja">

      <activity android:name=".StandaloneActivity" android:exported="true">
          <intent-filter>
              <action android:name="android.intent.action.MAIN"/>
              <category android:name="android.intent.category.LAUNCHER"/>
          </intent-filter>
      </activity>

      <!-- Deja tu MainActivity y demás tal cual, sin intent-filter LAUNCHER -->
  </application>
</manifest>