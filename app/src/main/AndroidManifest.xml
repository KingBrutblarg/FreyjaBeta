android {
    namespace = "com.kingbrutblarg.freyjabeta"   // <-- cámbialo si quieres otro
    compileSdk = 34

    defaultConfig {
        applicationId = "com.kingbrutblarg.freyjabeta" // que coincida con tu namespace deseado
        minSdk = 24
        targetSdk = 34
        // versionCode / versionName como los tengas
    }

    signingConfigs {
        create("release") {
            val ksFile  = project.findProperty("RELEASE_STORE_FILE") as String?
            val ksPass  = project.findProperty("RELEASE_STORE_PASSWORD") as String?
            val keyAl   = project.findProperty("RELEASE_KEY_ALIAS") as String?
            val keyPass = project.findProperty("RELEASE_KEY_PASSWORD") as String?
            if (ksFile != null && ksPass != null && keyAl != null && keyPass != null) {
                storeFile = file(ksFile)      // "freyja-release.p12" en /app
                storePassword = ksPass
                keyAlias = keyAl
                keyPassword = keyPass
                storeType = "pkcs12"
            }
        }
    }

    buildTypes {
        getByName("release") {
            isMinifyEnabled = false
            signingConfig = signingConfigs.getByName("release")
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }
        getByName("debug") { isDebuggable = true }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions { jvmTarget = "17" }
    buildFeatures { viewBinding = true }
}
