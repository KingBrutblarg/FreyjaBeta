<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema real de la app -->
    <style name="Theme.Freyja" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- (Opcional) status bar transparente si usas Compose full-screen -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
